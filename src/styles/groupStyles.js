import { StyleSheet } from 'react-native';

const groupStyles = StyleSheet.create({
    container: {
        margin: 8,
        padding: 10,
        borderRadius: 10,
    },
    title: {
        fontSize: 25,
        fontWeight: 'bold',
        color: 'white'
    },
    subtitle: {
        color: 'white'
    },
    description: {
        color: 'white',
        fontSize: 15
    }
});

export default groupStyles;