import { StyleSheet } from 'react-native';

const groupStyles = StyleSheet.create({
    container: {
        margin: 8,
        padding: 10,
        borderRadius: 10
    },
    title: {
        fontSize: 25,
        fontWeight: 'bold',
        color: 'black'
    }
});

export default groupStyles;